#include "gtest/gtest.h"

#include "data_structures/matrix.h"

TEST(MATRIX_TEST, initializing_matrix_with_data)
{
    // how do I want to initialize a matrix?
    // raytracer::matrix(rows, cols)
    // raytracer::matrix(data) --- data is a std::vector<std::vector<double>>
    // raytracer::matrix { std::vector<double>, std::vector<double> }
    // or all the above?
}
